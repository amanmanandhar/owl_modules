<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="throttle_debounce.list">
      <field name="name">throttle_debounce list</field>
      <field name="model">throttle_debounce.throttle_debounce</field>
      <field name="arch" type="xml">
        <list>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </list>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="throttle_debounce.action_window">
      <field name="name">throttle_debounce window</field>
      <field name="res_model">throttle_debounce.throttle_debounce</field>
      <field name="view_mode">list,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="throttle_debounce.action_server">
      <field name="name">throttle_debounce server</field>
      <field name="model_id" ref="model_throttle_debounce_throttle_debounce"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "list,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="throttle_debounce" id="throttle_debounce.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="throttle_debounce.menu_1" parent="throttle_debounce.menu_root"/>
    <menuitem name="Menu 2" id="throttle_debounce.menu_2" parent="throttle_debounce.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="throttle_debounce.menu_1_list" parent="throttle_debounce.menu_1"
              action="throttle_debounce.action_window"/>
    <menuitem name="Server to list" id="throttle_debounce" parent="throttle_debounce.menu_2"
              action="throttle_debounce.action_server"/>
-->
  </data>
</odoo>
